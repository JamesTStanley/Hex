<Window x:Class="WpfDisplaySample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="650" Width="800">
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Canvas Grid.Row="0" x:Name="CenterCanvas" HorizontalAlignment="Center" VerticalAlignment="Center" />
        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <Button x:Name="DrawAFlatTopHexGrid" Click="DrawAFlatTopHexGrid_Click" Content="Draw a Flat Top Hex Grid"/>
            <Button x:Name="DrawAPointyTopHexGrid" Click="DrawAPointyTopHexGrid_Click" Content="Draw a Pointy Top Hex Grid"/>
        </StackPanel>
    </Grid>
</Window>
